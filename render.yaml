services:
  - type: web
    name: bitespeed-api
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    plan: free
    region: ohio
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
